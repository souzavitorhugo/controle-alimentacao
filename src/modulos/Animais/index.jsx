import { Fragment, useEffect, useState, useMemo } from 'react';

import Table from '../../components/Table/index';

export default function AnimaisContainer() {
    const [data, setData] = useState({});

    useEffect(() => {

    }, [])
 
    return (
      <div>
        {/* <Table/> */}
        Animais
      </div>

    )
}